<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>GuideDialog</class>
 <widget class="QDialog" name="GuideDialog">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>600</width>
    <height>450</height>
   </rect>
  </property>
  <property name="minimumSize">
   <size>
    <width>600</width>
    <height>450</height>
   </size>
  </property>
  <property name="maximumSize">
   <size>
    <width>600</width>
    <height>450</height>
   </size>
  </property>
  <property name="windowTitle">
   <string>사용 가이드</string>
  </property>
  <layout class="QVBoxLayout" name="verticalLayout">
   <item>
    <widget class="QLabel" name="label_title">
     <property name="text">
      <string>📘 에어콕 프로그램 사용 가이드</string>
     </property>
     <property name="alignment">
      <set>Qt::AlignCenter</set>
     </property>
     <property name="styleSheet">
      <string notr="true">font-weight: bold; font-size: 16px;</string>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QTextEdit" name="text_guide">
     <property name="readOnly">
      <bool>true</bool>
     </property>
     <property name="styleSheet">
      <string notr="true">font-size: 13px;</string>
     </property>
     <property name="text">
      <string notr="true">
      데이터 보정 기능 사용법.

      1. 기준 장비 데이터와 에어콕 데이터를 선택합니다. (에어콕 파일은 여러 개 선택 가능)
      2. 'Calibration' 버튼을 눌러 제품 보정을 진행합니다.
      3. 출력된 보정 결과를 확인하여 제품 보정을 진행합니다. (이전/다음 버튼으로 탐색 가능)

      보정 결과 생성:
      → 제품별 보정값이 한 번에 정리되어 Excel 파일로 저장됩니다.

      보고서 생성:
      → 데이터를 병합하여 Excel 파일로 저장됩니다.
      → 파일 구조는 "데이터 비교", "데이터 평균값", "원본 데이터"로 구성됩니다.

      누적 보정값 적용:
      → 2회 이상 보정을 진행할 경우 기존에 적용된 보정값 데이터를 선택합니다.
      → 새롭게 보정한 값과 기존에 적용된 보정값이 누적 합산되어 출력됩니다.

      Aircok Log Converter:
      → 에어콕 LCD 제품의 TXT 로그 데이터를 CSV 파일로 변환합니다.

      Aircok Data Extractor:
      → 에어콕 제품의 데이터를 다운로드합니다.
      → 서버, SN, 기간, 센서를 선택하여 다운로드하며,
      → *생각공장 815호, 816호에서만 DB 연결이 가능합니다.*
      </string>
     </property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
